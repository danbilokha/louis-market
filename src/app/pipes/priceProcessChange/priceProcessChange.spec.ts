import {PriceProcessChangePipe} from './priceProcessChange';

describe('PriceProcessChangePipe', () => {
    
    let sut: PriceProcessChangePipe;

    beforeEach(() => {
        sut = new PriceProcessChangePipe();
    })

    describe('transform', () => {
        
    })

})